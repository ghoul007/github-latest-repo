## Install Packages

```
// devDependencies
npm i @types/inquirer @types/node @types/ora @types/puppeteer prettier rimraf semantic-release tslint typescript

//dependencies
npm i chalk inquirer ora puppeteer